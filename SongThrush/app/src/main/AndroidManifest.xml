<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.songthrush">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MySpaceActivity"></activity>
        <activity android:name=".ArtistsTracksActivity" />
        <activity android:name=".ArtistCatalogActivity" />
        <activity android:name=".NewReleaseActivity" />
        <activity android:name=".PlayerActivity" />
        <activity android:name=".PlaylistTracksActivity" />
        <activity android:name=".CategoryPlaylistActivity" />
        <activity android:name=".SongDetailsActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name="com.spotify.sdk.android.authentication.LoginActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>